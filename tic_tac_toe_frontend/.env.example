# Environment variables for Tic Tac Toe AI opponent
# IMPORTANT: Do not commit your real API keys to source control.
# In the preview/deployment environment, ensure this is injected securely.

# OpenAI API Key used by the frontend to call OpenAI APIs.
# In production, route this through a backend. For this exercise, the platform injects it securely.
REACT_APP_OPENAI_API_KEY=
